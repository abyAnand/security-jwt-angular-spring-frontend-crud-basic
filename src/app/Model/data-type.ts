export interface User {
    name: string;
    email: string;
    password: string;
    address: string;
    role: string; // Assuming role is represented as a string
  }

export interface TheUser{
  userId:number
  name: string;
  email: string;
  password: string;
  address: string;
  role: string;
}
